[project]
name = "pyphm"
version = "0.0.4"
description = "Machinery data, made easy"
requires-python = ">=3.6"

[build-system]
requires = ["setuptools~=60.9.2", "wheel"]
build-backend = "setuptools.build_meta"

[project.optional-dependencies]
doc = ["sphinx~=4.4.0", "myst-parser"]
